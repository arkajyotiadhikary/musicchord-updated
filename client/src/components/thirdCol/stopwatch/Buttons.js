const Buttons = () => {
    return (
        <div className="pomodoro-buttons d-flex justify-content-center">
            <button>Pomodoro</button>
            <button className="mx-4">Break</button>
            <button>Long Break</button>
        </div>
    );
};

export default Buttons;
